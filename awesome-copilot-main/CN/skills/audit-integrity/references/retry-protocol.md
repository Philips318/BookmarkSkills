#重试协议

关于工具故障或空结果：

1. **重试一次**与一个改进的查询或不同的搜索模式。
2. **如果第二次尝试失败**，明确说明失败的原因，并根据现有证据继续。
3. 永远不要因为一个工具调用没有返回结果而静默地跳过一个阶段——区分“工具没有找到任何东西”和“工具执行失败”。
4. **记录差距**：如果一个阶段确实被阻塞了（缺少清单，不支持的语言，无法访问的文件），在输出中显式地说明它，而不是默默地忽略该阶段。