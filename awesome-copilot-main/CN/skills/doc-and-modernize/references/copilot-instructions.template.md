#副驾驶说明-[项目名称]

> **由doc-and- Modernization技能（现代化模式）生成，作为可编辑的起点
>保存到**`.github/copilot-instructions.md`**(路径GitHub Copilot>自动负载)。它编码“规范命令”和“相位门控规则”
>代理（或人工）在执行`MODERNIZATION_PLAN.md`时必须遵循。调整
>将门、命令和分支规则添加到您的项目中—下面的占位符
>故意是通用的。自定义后删除此备注。[项目名称]正在**现代化**。安全策略是自适应的
遗留堆栈可能部分或全部失效，因此我们不假设它是完全绿色的
遗留CI门存在于前面。每个组件都位于** *可达到的最高级别
的安全梯** (L0-L4)，有一个名为**可测试性里程碑**(阶段
它首先在哪里构建、运行，并通过一个支持的工具链上的≥1个测试)，并且是
标记为**前测试性（“暗”）**或**后测试性（“亮”）**。CI是站着的
在一个命名为CI的里程碑（第一个点亮的阶段）；强制执行它是必需的
检查是人工操作的步骤。阶段路线图见`MODERNIZATION_PLAN.md`。
每个组件testability/rung， CI里程碑和剩余风险寄存器；`ARCHITECTURE.md`表示审计的当前状态。一次只做一个阶段；不
向前推进，直到当前阶段的退出标准得到明显满足。

# #命令>从架构文档的**命令和验证清单**中填写。使用
>你的生态系统真正的跑者(npm/yarn/pnpm,`make`,`just`,`cargo`,`go`，
>`poetry`/`tox`/`nox`,`gradle`/`mvn`，…)删除不适用的行。

|动作|命令||--------|---------|
|安装/恢复deps |`<install command>`|
|构建|`<build command>`|
|运行/ serve local |`<run command>`|
|单元测试（全部）|`<test command>`|
|单个测试文件|`<single-file test command>`|
|单个测试名称|`<single-test-by-name command>`|
| |`<lint command>`|
|格式/检查|`<format command>`/`<format-check command>`|
|类型检查（如果适用）|`<typecheck command>`|
|端到端/冒烟（如果适用）|`<e2e command>`|
|合同/特性测试|`<contract test command>`|

CI （`<path to CI workflow>`）运行<有序门列表，例如：lint→typecheck→
test→e2e>` on `<runtime/version>’为每一次push和PR. CI起立
**相位`<N>`**（第一个*点亮*相位）。**执行是一个单独的手册
步骤：**直到有人把这个工作流变成**需要的状态检查
分支保护规则**(' <平台：例如：GitHub→Settings→Branches
受保护的分支> ')，CI *运行*在pr上，但**不**阻止*合并。> **保持这个表是最新的。**如果一个阶段引入了一个新的长期命令，
>更新此表（并在规划时与用户确认）-不要离开
>规范命令列表陈旧。

阶段门控（状态感知；适用于每个阶段）

在“验证和退出标准”中，一个阶段才算“完成”`MODERNIZATION_PLAN.md`通过。这些标准必须是：

1. **客观可验证** -可运行的命令/捕获的证据，而不是一个
主观的判断。
2. **实际执行并记录** -运行它们并在计划中记录结果
在开始下一阶段之前。
3. **门控** - ** ** **不**进入N+1阶段，直到第N阶段的标准
明显。

哪些标准是有效的取决于组件的制度- **测试后（“点亮”）阶段：**退出可运行命令/ **绿色CI打开
相位为branch/PR-权威信号。**共享基线：' <lint
命令>` and `<测试命令> '必须通过；如果更改触及客户端/
传输/协议/任何公共接口，相关的端到端和contract/characterization测试也必须保持绿色。
- **预测试性（“黑暗”）阶段：**组件还没有活，所以是绿色的
CI测试门是一个**类错误**。退出阶段是可以实现的
**安全梯梯级**代替-捕获seam/oracle快照，可逆性
脚手架已被证实存在，烟雾检查表已通过，并有记录。* *不
在还不能运行的测试套件上阻止一个黑暗阶段。幸运的降级
到较低的级别（指定了剩余风险）是有效的退出，而不是失败。**永远不要在组件通过自己的自动测试门之前要求组件的自动测试门
可测试性的里程碑。**向用户报告pass/fail未知的相位
而不是假设它通过了。

# #决定

-当计划一个阶段时，必须解决所有子决策并形成文件
在计划中，实现不需要进一步的用户输入。
——国家* *“下降”和“递延”* *明确任何削减——不要离开它
模糊。
-更新`MODERNIZATION_PLAN.md`状态标记（✅complete /⏭️descoped /）
🗑️删除)，因为每个阶段的退出条件都满足，并且**更新
安全梯级/剩余风险登记册**当组件越过他们的
可测试性的里程碑。

##分支& pr每个阶段都是在自己的分支上开发的——永远不要直接提交阶段工作
到默认分支（`<default branch>`）。创建一个新的分支
相位（如`phase-N-<short-name>`）。一旦阶段的退出标准得到满足，
记录，推送分支并打开一个PR到默认分支。为
**测试后（“点亮”）**阶段，让CI对PR是权威的绿色
合并前的信号。对于预测试阶段（“暗”阶段），PR代替
携带可实现的安全阶梯的证据（捕获的合同，烟雾）
结果，可逆性脚手架)与剩余风险命名。**在下一阶段之前合并到主干；从来没有堆栈。**切断各相支路
**从trunk (`<default branch>`)**， **合并它的PR到trunk之前
开始下一阶段。**永远不要把一个相位分支或它的PR建立在一个同级分支上`phase-N`分支-堆叠的相位pr相互合并，成为主干
无声地拖延了几个阶段，后面的阶段被迫陷入困境。
在开始一个阶段之前，请检查前一个阶段是否合并`git log origin/<default branch>..HEAD`在创建分支时为空。* *控制
只有在真正不可避免的情况下才允许堆叠，而且必须经过调解
PR，使堆栈落在主干上，加上一个明确的剩余风险说明> **确认中继名称；取消遗留的默认值。别上真正的树干
> (`<default branch>`)。如果存在第二个默认分支(例如遗留
>`master`和`main`)，标记为“仅历史-不目标”
>工作永远不会落错地方。

**让可执行文档与代码保持一致。**任何改变的阶段
**拓扑** -module/reactor成员，删除服务，重命名分支，
更改命令，new/removed端点-必须更新**此文件**`README`，以及同一PR中的任何module/topology列表**。陈旧的模块列表或
硬编码的旧分支链接会主动误导下一个agent/human.**注册过渡不安全状态。**如果一个阶段有意引入
保持可运行的临时弱状态（permit-all shim， CSRF禁用，打开）
端点，一个占位符秘密)，用**关闭**和的阶段记录它
一个设计到第n阶段的笔记，并尽可能严格地限定它的范围——这个
抢占重复出现的scanner/reviewer混乱。

CI实施是一个手动的、仅限人工的步骤。**代理可以编写和运行
> CI工作流，但使其成为**必需的状态检查/分支保护
>规则**所以它实际上*块*合并是一个platform-UI/admin动作
> (`<GitHub → Settings → Branches; GitLab protected branches; etc.>`)代理
>无法执行。在您配置它之前，请将“绿色CI是权威的”视为一个错误
您手动执行的>约定。这样做一次，at/afterCI Milestone阶段。