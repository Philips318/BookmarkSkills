---
description: 'Guidelines for building C# applications'
applyTo: '**/*.cs'
---
c#开发

c#指令
-始终使用最新版本的c#，目前c# 14的特性。
—为每个函数编写清晰简洁的注释。

##一般使用说明
-在审查代码更改时只提出高可信度的建议。
编写具有良好可维护性的代码，包括对做出某些设计决策的原因的注释。
—处理边缘情况，编写清晰的异常处理。
-对于库或外部依赖，在注释中提到它们的用途和目的。

命名约定

—组件名、方法名和公共成员遵循PascalCase。
-私有字段和局部变量使用camelCase。
-以“I”作为接口名称的前缀（例如，IUserService）。

# #格式化—采用`.editorconfig`中定义的代码格式化样式。
-首选文件作用域命名空间声明和单行using指令。
-在任何代码块的左花括号之前插入一个换行符（例如，在`if`、`for`、`while`、`foreach`、`using`、`try`等之后）。
-确保方法的最终返回语句在自己的行上。
-尽可能使用模式匹配和转换表达式。
—在引用成员名时使用`nameof`而不是字符串字面值。
-确保为任何公共api创建XML文档注释。如果适用，在注释中包括`<example>`和`<code>`文档。

##项目设置和结构—指导用户创建新用户。使用适当的模板创建。NET项目。
-解释每个生成文件和文件夹的目的，以建立对项目结构的理解。
-演示如何使用功能文件夹或领域驱动设计原则组织代码。
-对模型、服务和数据访问层进行适当的关注点分离。
-讲解ASP中的Program.cs和配置系统。包括特定于环境的设置。

可空引用类型

-声明变量不可为空，并在入口点检查`null`。
-总是使用`is null`或`is not null`，而不是`== null`或`!= null`。
信任c#的null注释，当类型系统说值不能为空时，不要添加null检查。

##数据访问模式-使用实体框架核心指导数据访问层的实现。
-解释不同的开发和生产选项（SQL Server, SQLite, In-Memory）。
-演示存储库模式的实现以及它何时是有益的。
-展示如何实现数据库迁移和数据播种。
-解释有效的查询模式，以避免常见的性能问题。

##认证和授权

—指导用户使用JWT承载令牌实现身份验证。
-解释与ASP相关的OAuth 2.0和OpenID Connect概念。净的核心。
—展示基于角色和策略授权的实现方法。
-演示与Microsoft Entra ID（以前的Azure AD）的集成。
-解释如何保证基于控制器和最小api的一致性。

验证和错误处理-使用数据注释和FluentValidation指导模型验证的实现。
-解释验证管道和如何自定义验证响应。
-演示一个使用中间件的全局异常处理策略。
-展示如何跨API创建一致的错误响应。
-解释标准化错误响应的问题细节（RFC 9457）实现。

API版本控制和文档

-指导用户实现和解释API版本控制策略。
-用适当的文档演示Swagger/OpenAPI的实现。
—展示如何记录端点、参数、响应和身份验证。
-解释基于控制器和最小api的版本控制。
-指导用户创建有意义的API文档，帮助消费者。

##日志和监控-使用Serilog或其他提供商指导结构化日志的实现。
-解释日志级别和何时使用每个级别。
-演示与遥测采集应用程序的集成。
-展示如何实现自定义遥测和相关id的请求跟踪。
-解释如何监控API性能、错误和使用模式。

# #测试

-始终包含应用程序关键路径的测试用例。
—指导用户创建单元测试。
-不要发出“Act”、“Arrange”或“Assert”注释。
-复制现有的样式在附近的文件测试方法的名称和大写。
-解释API端点的集成测试方法。
演示如何模拟依赖关系以进行有效的测试。
—展示如何测试认证和授权逻辑。
-解释应用于API开发的测试驱动开发原则。性能优化

—指导用户实现缓存策略（内存缓存、分布式缓存、响应缓存）。
-解释异步编程模式以及为什么它们对API性能很重要。
-演示大型数据集的分页、过滤和排序。
-展示如何实现压缩和其他性能优化。
-解释如何测量和基准API性能。

##部署和DevOps-指导用户使用容器化API。NET的内置容器支持（`dotnet publish --os linux --arch x64 -p:PublishProfile=DefaultContainer`）。
—说明手动创建Dockerfile和手动创建Dockerfile的区别。. NET的容器发布特性。
-解释。NET应用程序的CI/CD管道。
-演示部署到Azure应用服务、Azure容器应用程序或其他托管选项。
-演示如何实施健康检查和准备状态探测。
-解释不同部署阶段的环境特定配置。