---
description: 'C# アプリケーション構築指針 by @tsubakimoto'
applyTo: '**/*.cs'
---
# c #

## c #
# # # # # #c# 14（英文）
-。

##
-。
——なぜその設計判断をしたのかという理由を含むコメントなど,保守性の高い実践に沿ってコードを書きます。
-。
——ライブラリや外部依存については,用途と目的をコメントで明記します。

## 命名規則

-。
——プライベートフィールドとローカル変数にはcamelCaseを使用します。
-“I”（IUserService）。

##

-`.editorconfig`。
——ファイルスコープの名称空间宣言と1行の使用ディレクティブを推奨します。
——任意のコードブロック(如果,foreach、使用、尝试など)の開始波括弧の前に改行を入れます。
★★★★★★★★★★★★★★★★★
-。
——メンバー名参照には文字列リテラルではなく`nameof`を使用します。
-。`<example>``<code>`。

##-。Net。
——生成される各ファイルとフォルダーの目的を説明し,プロジェクト構造の理解を助けます。
-。
-。
- ASP。. NET Core 10,Program.cs，。

## Nullable

-。
——`== null`や`!= null`ではなく,常に`is null`または`is not null`を使用します。
- C #の空注釈を信頼し,型システムが値の非零を保証している場合は不要な零チェックを追加しません。

##

-实体框架核心
——開発および本番における選択肢(SQL Server、SQLite、内存)を説明します。
-。
-。
-。

##

- JWT。
- ASP。网络核心に関連するOAuth 2.0およびOpenID连接の概念を説明します。
-。
- Microsoft Entra ID（Azure AD）。
——コントローラーベースAPIと最小APIの双方を一貫して保護する方法を説明します。

## #——データ注釈とFluentValidationを用いたモデル検証の実装を案内します。
-。
-。
- API：。
——標準化されたエラー応答のための问题细节(RFC 9457)の実装を説明します。

## API，

- API。
-
-。
——コントローラーベースAPIと最小APIの双方でのバージョニングを説明します。
——利用者に役立つ有意義なAPIドキュメントの作成を案内します。

##

- - - - - - - - - - - - - - - - - - -
-。
-。
——リクエスト追跡のためのカスタムテレメトリと相関IDの実装方法を示します。
- API。

##

-。
★★★★★★★
-“行动”，“安排”，“主张”，。
★★★★★★★★★★★★★★★★★
- API。
-。
-。
- API：。

##-。
——非同期プログラミングパターンと,それがAPIのパフォーマンスにおいて重要である理由を説明します。
-。
——圧縮などのパフォーマンス最適化の実装方法を示します。
- API。

DevOps

-。净の組み込みコンテナーサポート(`dotnet publish --os linux --arch x64 -p:PublishProfile=DefaultContainer`)を用いたAPIのコンテナー化を案内します。
- Dockerfile（文件）网络：。
-。CI/CD。
——Azure应用服务,Azure容器应用程序,その他のホスティングオプションへのデプロイを示します。
-准备就绪探测器（ready Probe）
-姥姥，姥姥，姥姥，姥姥，姥姥，姥姥