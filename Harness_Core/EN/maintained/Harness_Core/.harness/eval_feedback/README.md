# Evaluator Feedback

This directory stores evaluator verdict JSON files. Each file is named `{plan-slug}_{task-id}.json`.

Files are created automatically by the evaluation workflow and should not be manually edited.
